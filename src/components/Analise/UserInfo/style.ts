import styled from "styled-components";
import 'theme/variables.css'

export const Container = styled.div`
  margin-left: 10px;

  h3 {
    font-size: 16px;
    font-weight: 700;
  }

  p {
    color: var(--laranja);
    font-size: 16px;
    font-weight: 400;
    margin-bottom: 30px;
  }
`