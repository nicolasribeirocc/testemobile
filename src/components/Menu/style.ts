import styled from "styled-components";

export const Container = styled.div`
  ion-toolbar {
    --background: var(--azul);
 }
 ion-title {
  color: #fff;
 }
 ion-menu-button {
  color: var(--laranja);
 }
`